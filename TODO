The ci crate never uses the project_id and we have a nasty TODO because
of it.

    // TODO
    let project_id = 42;

Week-end in French is seen as a bad word - and I don't want to
add both week and end to the global ignore list for French 🤔

skyspell can see `f'input` as been one token when tokenizing Python

skyspell-ci: add `run` and `init` sub commands, where `init` does almost what the
InteractiveChecker does, except it generates a valid skyspell.yml file at the end
