The ci crate never uses the project_id and we have a nasty TODO because
of it.

    // TODO
    let project_id = 42;

Week-end in French is seen as a bad word - and I don't want to
add both week and end to the global ignore list for French 🤔

skyspell can see `f'input` as been one token when tokenizing Python. Ditto with `b'word'`

skyspell-ci init:
  * write more frequently
  * don't crash on non-utf-8 files - maybe the first step of `skyspell-ci run` should be
    to list non-utf-8 files?
